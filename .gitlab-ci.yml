#services:
    #-docker

stages:
    - build
    - test
    - deploy

build:
    stage:  build
    script:
        - echo  "Building"
        - mkdir build
        - touch build/123.txt

test:
    stage: test
    script:
        - echo  "Testing"
        - test -f "build/123.txt"

deploy:
    stage: deploy
    script:
        - pybot -d Results --reporttitle "NOMBRE PARA EL REPORTE" --logtitle "NOMBRE PARA EL LOG" Lectulandia.robot
