stages:
    - build
    - test


build:
    stage:  build
    script:
        - robot -d Results --reporttitle "NOMBRE PARA EL REPORTE" --logtitle "NOMBRE PARA EL LOG" Lectulandia.robot

test:
    stage: test
    script:
        - cat Results/log.html
